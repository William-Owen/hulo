# Hulo
Human Log

## Install

`npm install hulo -g`

## Log

`hulo log <message>`

Example:

`hulo log "Hello world"`

## Last

`hulo last`

Example:

`hulo last <number of items>`

Example, will return the last three elements:

`hulo last 3`
